#pragma once
#define __QOR_TRANSIENT_RESOURCE_ALLOCATOR_SRC__

#include "rendering_env.h"

// Manage resources that endure within a frame
struct __TransientResourceAllocator
{
    
};
typedef struct __TransientResourceAllocator _TransientResourceAllocator;
